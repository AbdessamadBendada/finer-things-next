# legal

Privacy and terms.

Both are placeholder text pending legal review, marked `noindex` and excluded
from the sitemap. `useReadingIndex` highlights the current section in the side
index as the reader scrolls. See docs/adr/0002-deferred-compliance.md.
