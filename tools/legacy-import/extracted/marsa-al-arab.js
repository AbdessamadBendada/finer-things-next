/* Extracted from legacy/marsa-al-arab.html. Reference only — port into hooks. */

const reveal=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');reveal.unobserve(e.target)}}),{threshold:.14,rootMargin:'0px 0px -6%'});document.querySelectorAll('.rise').forEach(el=>reveal.observe(el));const chapters=[...document.querySelectorAll('.chapter')],chapterIO=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in-view')}),{threshold:.16});chapters.forEach(c=>chapterIO.observe(c));
if(!matchMedia('(prefers-reduced-motion:reduce)').matches){let frame=false;const update=()=>{document.querySelector('.hero-bg').style.setProperty('--hero-y',(Math.min(1,scrollY/innerHeight)*34)+'px');chapters.forEach(c=>{const r=c.getBoundingClientRect(),p=Math.max(0,Math.min(1,(innerHeight-r.top)/(innerHeight+r.height)));c.style.setProperty('--chapter-y',((p-.5)*38)+'px');c.style.setProperty('--chapter-no-y',((p-.5)*-22)+'px');c.classList.toggle('passing',r.top<0&&r.bottom<innerHeight*.72)});document.querySelectorAll('[data-drift]').forEach(s=>{const r=s.getBoundingClientRect(),p=Math.max(0,Math.min(1,(innerHeight-r.top)/(innerHeight+r.height)));s.style.setProperty('--shot-y',((p-.5)*Number(s.dataset.drift))+'px')});frame=false};const queue=()=>{if(!frame){requestAnimationFrame(update);frame=true}};addEventListener('scroll',queue,{passive:true});addEventListener('resize',queue);update()}

